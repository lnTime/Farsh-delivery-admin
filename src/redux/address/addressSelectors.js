export const getCountriesSelector = state => state.address.countries;

export const getStatesSelector = state => state.address.states;

export const getCitiesSelector = state => state.address.cities;

export const getAddressSelector = state => state.address;